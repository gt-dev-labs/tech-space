# Labs — 02 Processes

Companion notes: [`notes/02-processes/notes.md`](../../notes/02-processes/notes.md)

1. [`01-pid-info`](01-pid-info) — inspect PID/PPID and the memory layout (text/data/bss/heap/stack) from inside a running process
2. [`02-fork-basics`](02-fork-basics) — `fork()` and copy-on-write
3. [`03-fork-exec`](03-fork-exec) — the fork + exec + wait pattern every shell uses
4. [`04-zombie-orphan`](04-zombie-orphan) — deliberately create and observe a zombie and an orphan process
5. [`05-mini-shell`](05-mini-shell) — capstone: a tiny interactive shell built from the pieces above

Work through them in order — each one builds on the last.
