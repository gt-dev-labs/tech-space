# Docker & Kubernetes

Container and orchestration architecture — how Docker and Kubernetes actually work, not just how to drive their CLIs. A curated plan through the shared topic pool in `../topics/`.

## Plan

1. Docker & Kubernetes architecture — *not built yet*

**Builds on:** a future Namespaces & cgroups topic in the [operating-systems](operating-systems.md) plan (not yet built) — Docker/Kubernetes don't introduce a new isolation mechanism, they orchestrate the same kernel primitives that topic will cover. This plan picks up from there rather than re-deriving it.

Originally planned as a capstone of the operating-systems plan, split out into its own plan once it became clear it deserved its own roadmap rather than being the endpoint of an OS-internals sequence.
