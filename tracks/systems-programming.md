# Systems Programming

General programming-craft/build-tooling topics that aren't specific to any one OS or language runtime. A curated plan through the shared module pool in `../notes/` and `../labs/`.

## Plan

1. [Toolchain: compiling and running C by hand](../notes/toolchain/notes.md) — labs: [`labs/toolchain`](../labs/toolchain)

Room to grow later with things like build systems, debuggers, profilers, or cross-compilation, as they come up. OS-internals content (processes, memory, syscalls, ...) lives in the [operating-systems](operating-systems.md) plan instead.
